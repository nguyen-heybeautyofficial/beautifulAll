import * as React from 'react';
import { StyleSheet } from 'react-native';
import { settingApp, colorApp } from '../../public';

const styles = StyleSheet.create({
    main:{
        flex:1,
        backgroundColor:colorApp.while
    }
})

export default styles